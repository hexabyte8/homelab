# Fail2ban Report — {{ f2b_report_date }}

Generated by GitHub Actions on {{ ansible_date_time.iso8601 | default(f2b_report_date) }}.

## Summary

| Node | Jail | Currently Banned | Total Banned (all time) |
|------|------|-----------------|------------------------|
{% for entry in f2b_parsed_results %}
| {{ entry.node }} | {{ entry.jail }} | {{ entry.currently_banned }} | {{ entry.total_banned }} |
{% endfor %}

## Currently Banned IPs

{% for entry in f2b_parsed_results %}
### {{ entry.node }} — {{ entry.jail }}

{% if entry.banned_ips %}
| IP Address |
|------------|
{% for ip in entry.banned_ips %}
| `{{ ip }}` |
{% endfor %}
{% else %}
_No IPs currently banned._
{% endif %}

{% endfor %}
---

**Total IPs currently banned across all nodes/jails: {{ f2b_parsed_results | map(attribute='banned_ips') | map('length') | sum }}**
