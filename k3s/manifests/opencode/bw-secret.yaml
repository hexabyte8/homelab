# BitwardenSecret — opencode-secret
#
# One-time setup:
#   1. Create the following entry in BWS (sm.bitwarden.com).
#   2. Replace the placeholder UUID below before merging this deployment.
#   3. After Flux creates the namespace, run the k3s-patch-secrets workflow
#      with target `bw-auth-token` so the operator can sync this secret.
#
#   OPENCODE_SERVER_PASSWORD  value: (a strong password for the web interface)
apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: opencode-secret
  namespace: opencode
spec:
  organizationId: "5f82d531-e61f-4c86-963c-b40f00c51c93"
  secretName: opencode-secret
  authToken:
    secretName: bw-auth-token
    secretKey: token
  onlyMappedSecrets: true
  projectId: "aece2880-f0d0-4a77-9b0c-b40f00c78f1e"
  map:
    - bwSecretId: "e8a69694-0e68-42e0-b5f0-b4c101538b85"
      secretKeyName: OPENCODE_SERVER_PASSWORD
