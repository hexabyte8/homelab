apiVersion: v1
kind: Service
metadata:
  name: mcp-kubernetes
  namespace: mcp-kubernetes
spec:
  type: ClusterIP
  selector:
    app: mcp-kubernetes
  ports:
    - name: http
      port: 8000
      targetPort: 8000
