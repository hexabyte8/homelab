apiVersion: v1
kind: Service
metadata:
  name: actual-mcp-secondary
  namespace: actual-mcp-secondary
spec:
  type: ClusterIP
  selector:
    app: actual-mcp-secondary
  ports:
    - name: http
      port: 3000
      targetPort: 3000
