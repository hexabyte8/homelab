apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: actual-mcp-secondary
  namespace: actual-mcp-secondary
  annotations:
    tailscale.com/proxy-class: "prod"
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: actual-mcp-secondary
                port:
                  number: 3000
  tls:
    - hosts:
        - actual-mcp-secondary
