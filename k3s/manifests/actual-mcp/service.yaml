apiVersion: v1
kind: Service
metadata:
  name: actual-mcp
  namespace: actual-mcp
spec:
  type: ClusterIP
  selector:
    app: actual-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
