apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-mcp-data
  namespace: actual-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
