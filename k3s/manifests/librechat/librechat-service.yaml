apiVersion: v1
kind: Service
metadata:
  name: librechat
  namespace: librechat
spec:
  type: ClusterIP
  selector:
    app: librechat
  ports:
    - name: http
      port: 3080
      targetPort: 3080
