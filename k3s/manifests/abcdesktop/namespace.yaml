apiVersion: v1
kind: Namespace
metadata:
  name: abcdesktop
  labels:
    # abcdesktop user pods run as a specific non-root UID but require IPC and
    # X11 socket access; warn-level is sufficient here — the real enforcement
    # is handled by the container securityContext in the user pod template.
    pod-security.kubernetes.io/warn: baseline
