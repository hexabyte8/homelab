apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: monitoring
type: Opaque
stringData:
  # Patch with:
  #   kubectl patch secret grafana-admin-secret -n monitoring --type=merge \
  #     -p '{"stringData":{"admin-user":"admin","admin-password":"<password>"}}'
  admin-user: "REPLACE_ME"
  admin-password: "REPLACE_ME"
